Step file for **11.4.3** - Identity & Reachability (Merged Workspace Surface).
Load `../00_shared-contracts.mdc` first.

## Step Metadata
- Step ID: `11.4.3`
- Primary route: `(main)/patients/patients/[id]?tab=identity`
- Backend modules: `patient-identifier`, `patient-contact`, `patient-guardian`
- Action mode: In-workspace merged CRUD + compatibility redirects.

## Owned Route Surface
- Workspace tab/panels:
  - `/patients/patients/[id]?tab=identity&panel=identifiers`
  - `/patients/patients/[id]?tab=identity&panel=contacts`
  - `/patients/patients/[id]?tab=identity&panel=guardians`
- Legacy compatibility redirects:
  - `src/app/(main)/patients/patient-identifiers/**`
  - `src/app/(main)/patients/patient-contacts/**`
  - `src/app/(main)/patients/patient-guardians/**`

## Platform Screen Surface
- `src/platform/screens/patients/PatientWorkspaceScreen/**`
- `src/platform/screens/patients/components/**` (field help + inline guide)

## Test Surface
- `src/__tests__/app/(main)/patients-routes.test.js`
- `src/__tests__/platform/screens/patients/usePatientWorkspaceScreen.test.js`

## Implementation Granularity
- Implement identifiers/contacts/guardians as merged in-tab operations only.
- Do not re-introduce standalone list/detail/create/edit patient subresource screens.
- Keep role-guarded actions and entitlement-safe blocked UX.
- Keep identity display human-readable (`human_friendly_id`, labels); no raw UUID rendering.

## Completion Checklist
- [ ] Identity tab supports merged CRUD for identifiers/contacts/guardians.
- [ ] Legacy subresource routes redirect correctly with and without patient context.
- [ ] Help icon and inline field guide render for each editable field.
- [ ] Tests cover redirect mapping and action-level access behavior.

## Shared Contract Enforcement (Mandatory)
- This file inherits and must fully comply with ../00_shared-contracts.mdc.
- All mandatory contracts in ../00_shared-contracts.mdc are required for work executed from this prompt.
- ../00_shared-contracts.mdc has precedence over this file; if instructions conflict, the shared contract wins.
- A task is complete only when this file and every mandatory requirement in ../00_shared-contracts.mdc are satisfied.
