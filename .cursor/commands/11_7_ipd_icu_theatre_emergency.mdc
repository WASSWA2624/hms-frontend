Steps to review and implement:
- **11.7.1** IPD (main) -- `(main)/ipd`
- **11.7.2.1** IPD Admission -- `(main)/ipd/admissions`
- **11.7.2.2** IPD Bed assignment -- `(main)/ipd/bed-assignments`
- **11.7.2.3** IPD Ward round -- `(main)/ipd/ward-rounds`
- **11.7.2.4** IPD Nursing note -- `(main)/ipd/nursing-notes`
- **11.7.2.5** IPD Medication admin -- `(main)/ipd/medication-administrations`
- **11.7.2.6** IPD Discharge -- `(main)/ipd/discharge-summaries`
- **11.7.2.7** IPD Transfer -- `(main)/ipd/transfer-requests`
- **11.7.3** ICU (main) -- `(main)/icu`
- **11.7.4.1** ICU Stay -- `(main)/icu/icu-stays`
- **11.7.4.2** ICU Observation -- `(main)/icu/icu-observations`
- **11.7.4.3** ICU Critical alert -- `(main)/icu/critical-alerts`
- **11.7.5** Theatre (main) -- `(main)/theatre`
- **11.7.6.1** Theatre Case -- `(main)/theatre/theatre-cases`
- **11.7.6.2** Theatre Anesthesia record -- `(main)/theatre/anesthesia-records`
- **11.7.6.3** Theatre Post-op note -- `(main)/theatre/post-op-notes`
- **11.7.7** Emergency (main) -- `(main)/emergency`
- **11.7.8.1** Emergency Case -- `(main)/emergency/emergency-cases`
- **11.7.8.2** Emergency Triage -- `(main)/emergency/triage-assessments`
- **11.7.8.3** Emergency Response -- `(main)/emergency/emergency-responses`
- **11.7.8.4** Emergency Ambulance -- `(main)/emergency/ambulances`
- **11.7.8.5** Emergency Dispatch -- `(main)/emergency/ambulance-dispatches`
- **11.7.8.6** Emergency Trip -- `(main)/emergency/ambulance-trips`

Review and implement all missing and non-compliant aspects of the provided dev step(s) as per `hms-frontend/dev-plan/P011_screens-routes.md`.

You must ensure 100% compliance with:
- All rules in `hms-frontend/.cursor/rules/*`
- `hms-frontend/dev-plan/P011_screens-routes.md`
- `hms-frontend/write-up.md`
- Backend contracts in `hms-backend/dev-plan/P010_api_endpoints.mdc` for selected Tier 7 resources
- Actual mounted backend routes in `hms-backend/src/app/router.js` and these Tier 7 module route files:
  - `hms-backend/src/modules/admission/routes/admission.routes.js`
  - `hms-backend/src/modules/bed-assignment/routes/bed-assignment.routes.js`
  - `hms-backend/src/modules/ward-round/routes/ward-round.routes.js`
  - `hms-backend/src/modules/nursing-note/routes/nursing-note.routes.js`
  - `hms-backend/src/modules/medication-administration/routes/medication-administration.routes.js`
  - `hms-backend/src/modules/discharge-summary/routes/discharge-summary.routes.js`
  - `hms-backend/src/modules/transfer-request/routes/transfer-request.routes.js`
  - `hms-backend/src/modules/icu-stay/routes/icu-stay.routes.js`
  - `hms-backend/src/modules/icu-observation/routes/icu-observation.routes.js`
  - `hms-backend/src/modules/critical-alert/routes/critical-alert.routes.js`
  - `hms-backend/src/modules/theatre-case/routes/theatre-case.routes.js`
  - `hms-backend/src/modules/anesthesia-record/routes/anesthesia-record.routes.js`
  - `hms-backend/src/modules/post-op-note/routes/post-op-note.routes.js`
  - `hms-backend/src/modules/emergency-case/routes/emergency-case.routes.js`
  - `hms-backend/src/modules/triage-assessment/routes/triage-assessment.routes.js`
  - `hms-backend/src/modules/emergency-response/routes/emergency-response.routes.js`
  - `hms-backend/src/modules/ambulance/routes/ambulance.routes.js`
  - `hms-backend/src/modules/ambulance-dispatch/routes/ambulance-dispatch.routes.js`
  - `hms-backend/src/modules/ambulance-trip/routes/ambulance-trip.routes.js`
- If backend changes are required, full compliance with `hms-backend/.cursor/rules/*` is mandatory

Only implement what is required for the selected Tier 7 screen(s) to function correctly.

---

## Step Selection Contract (Mandatory)

- The request can target one or more steps from `11.7.1` to `11.7.8.6`.
- Group labels `11.7.2`, `11.7.4`, `11.7.6`, and `11.7.8` are collections, not single resources.
- If a request selects a group label explicitly, implement all child tab steps under that group.
- If a request selects specific child tab step IDs, implement only those.
- If a selected step depends on shared Tier 7 wiring, implement only the minimal shared wiring required.
- Do NOT auto-implement unselected sibling steps.

---

## Scope

Implement full functionality required for the selected Tier 7 step(s):

- Route + screen wiring under `(main)/ipd/*`, `(main)/icu/*`, `(main)/theatre/*`, `(main)/emergency/*` (selected steps only)
- Backend-aligned API integration for selected resources
- Loading, empty, error, offline, and success states
- Validation for required create/edit inputs
- List, detail, create, edit, and delete flows where backend supports them
- Navigation (Back / Cancel / Save / Continue) with no dead ends
- Parent-child flow continuity from each selected Tier 7 main screen to selected sub-resources
- Prefill and relationship handling from trusted context:
  - `tenant_id`, `facility_id`, `patient_id`, `encounter_id`, `admission_id`, `bed_id`, `nurse_user_id`, `prescription_id`, `from_ward_id`, `to_ward_id`, `icu_stay_id`, `theatre_case_id`, `anesthetist_user_id`, `emergency_case_id`, `ambulance_id`
- Role-feature access control for logged-in users on all selected Tier 7 routes/actions
- Accessibility control for all selected Tier 7 actions and route states

Frontend integration requirements for alignment with the current codebase:
- Reuse existing feature modules under `src/features/<resource>/*` for selected resources.
- Reuse existing hooks when present (`useEmergencyCase`, `useTriageAssessment`, `useEmergencyResponse`, `useAmbulance`, `useAmbulanceDispatch`, `useAmbulanceTrip`).
- If a selected Tier 7 resource hook is missing, create a minimal `useCrud` wrapper hook for that selected resource and export it in `src/hooks/index.js`.
- Avoid introducing new broad abstractions unless inevitable; prefer existing patients/scheduling/clinical screen patterns.

Tab UI guidance:
- Treat "tabs" in Tier 7 as route-backed sub-resource screens.
- Avoid adding new heavy tab systems unless inevitable.
- Prefer existing list/detail/create/edit route patterns and simple section navigation.

Do NOT implement steps outside the selected Tier 7 step(s).

---

## Backend Reality Check (Mandatory)

- Treat `hms-backend/src/app/router.js` + listed Tier 7 module route files as source of truth.
- Use only mounted top-level resources:
  - `/api/v1/admissions`
  - `/api/v1/bed-assignments`
  - `/api/v1/ward-rounds`
  - `/api/v1/nursing-notes`
  - `/api/v1/medication-administrations`
  - `/api/v1/discharge-summaries`
  - `/api/v1/transfer-requests`
  - `/api/v1/icu-stays`
  - `/api/v1/icu-observations`
  - `/api/v1/critical-alerts`
  - `/api/v1/theatre-cases`
  - `/api/v1/anesthesia-records`
  - `/api/v1/post-op-notes`
  - `/api/v1/emergency-cases`
  - `/api/v1/triage-assessments`
  - `/api/v1/emergency-responses`
  - `/api/v1/ambulances`
  - `/api/v1/ambulance-dispatches`
  - `/api/v1/ambulance-trips`
- Additional supported action endpoint:
  - `POST /api/v1/admissions/:id/discharge`

Respect actual filter support:
- Admissions: `tenant_id`, `facility_id`, `patient_id`, `encounter_id`, `status`
- Bed assignments: `admission_id`, `bed_id`
- Ward rounds: `admission_id`
- Nursing notes: `admission_id`, `nurse_user_id`
- Medication administrations: `admission_id`, `prescription_id`, `route`
- Discharge summaries: `admission_id`, `status`
- Transfer requests: `admission_id`, `from_ward_id`, `to_ward_id`, `status`, `search`
- ICU stays: `admission_id`, `started_at_from`, `started_at_to`, `ended_at_from`, `ended_at_to`, `is_active`
- ICU observations: `icu_stay_id`, `observed_at_from`, `observed_at_to`, `search`
- Critical alerts: `icu_stay_id`, `severity`, `search`
- Theatre cases: `encounter_id`, `status`, `scheduled_from`, `scheduled_to`
- Anesthesia records: `theatre_case_id`, `anesthetist_user_id`
- Post-op notes: `theatre_case_id`
- Emergency cases: `tenant_id`, `facility_id`, `patient_id`, `severity`, `status`
- Triage assessments: `emergency_case_id`, `triage_level`
- Emergency responses: `emergency_case_id`
- Ambulances: `tenant_id`, `facility_id`, `status`, `search`
- Ambulance dispatches: `ambulance_id`, `emergency_case_id`, `status`
- Ambulance trips: `ambulance_id`, `emergency_case_id`

Status/enum constraints that must match backend schema:
- Admission `status`: `ADMITTED`, `DISCHARGED`, `TRANSFERRED`, `CANCELLED`
- Discharge summary `status`: `PLANNED`, `COMPLETED`, `CANCELLED`
- Transfer request `status`: `REQUESTED`, `APPROVED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`
- Medication administration `route`: `ORAL`, `IV`, `IM`, `SC`, `TOPICAL`, `INHALATION`, `RECTAL`, `OTHER`
- Critical alert `severity`: `LOW`, `MEDIUM`, `HIGH`, `CRITICAL`
- Theatre case `status`: `SCHEDULED`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`
- Emergency case `severity`: `LOW`, `MEDIUM`, `HIGH`, `CRITICAL`
- Emergency case `status`: `PENDING`, `IN_PROGRESS`, `COMPLETED`, `CANCELLED`
- Triage `triage_level`: `IMMEDIATE`, `URGENT`, `LESS_URGENT`, `NON_URGENT`
- Ambulance and ambulance dispatch `status`: `AVAILABLE`, `DISPATCHED`, `EN_ROUTE`, `ON_SCENE`, `TRANSPORTING`, `OUT_OF_SERVICE`

Do not call frontend endpoint constants that are not mounted by backend routes.
Do not invent nested shortcut endpoints such as:
- `/api/v1/ipd/admissions`
- `/api/v1/admissions/:id/bed-assignments`
- `/api/v1/admissions/:id/ward-rounds`
- `/api/v1/admissions/:id/nursing-notes`
- `/api/v1/admissions/:id/medication-administrations`
- `/api/v1/icu/:id/critical-alerts`
- `/api/v1/theatre/:id/anesthesia-records`
- `/api/v1/emergency-cases/:id/triage`
- `/api/v1/emergency-cases/:id/dispatches`
- `/api/v1/ambulances/:id/trips`

Do not expand scope into unselected neighboring modules (for example diagnostics, pharmacy, inventory, billing, housekeeping) unless explicitly selected in a different task.
If backend support does not exist for a selected write action, provide read-only/informational UX and block unsupported writes.

---

## Backend Change Policy (Mandatory)

- Modify backend only when strictly required for selected Tier 7 step(s).
- Any backend modification must follow `hms-backend/.cursor/rules/*`.
- Preserve backend architecture and conventions (versioned routing, module structure, validation, response format, auth/security, tests).
- Do not introduce ad-hoc backend behavior that bypasses established module patterns.

---

## Access Control (Mandatory)

- Enforce access using existing auth/session and role-feature hooks/utilities/patterns already used in the app.
- Use a single source of truth per selected module for auth/session + role-feature access.
- Apply access checks to both:
  - UI actions (view/create/edit/delete/retry and action endpoints where applicable)
  - Route navigation (including direct deep links to selected Tier 7 routes)
- Never rely on UI hiding alone; route-level protection must prevent unauthorized access.
- Block unauthorized data actions before API calls and handle backend `401/403` responses with clear feedback.
- For unauthorized routes, redirect to nearest allowed main route and preserve safe navigation.

---

## Navigation + Routing Accessibility (Mandatory)

- Use semantic links for navigation and semantic buttons for actions; no clickable non-semantic containers.
- Ensure current location is perceivable via visible and accessible state.
- For visible-but-blocked actions, use `aria-disabled="true"` on web, prevent execution, and provide an accessible reason.
- Ensure keyboard navigation order, focus visibility, and focus restoration after submit/redirect/error.
- Provide screen-reader friendly feedback for loading, errors, denied actions, and successful actions.

---

## Layout Rules (Strict)

- Reuse existing Main Layout and route-group layout patterns for selected Tier 7 groups.
- If a selected Tier 7 route-group layout is missing, create it using existing composition patterns (no redesign).
- No layout redesign.
- No nested scroll containers.
- No nested sidebars.
- Match current main/settings/dashboard/patients/scheduling/clinical screens visually and structurally.

---

## UI / UX Rules

- Clean, compact, modern UI.
- Fully responsive (mobile, tablet, desktop, web, iOS, Android).
- Clear action hierarchy.
- Human-friendly copy via i18n keys only.
- No duplicate UI.
- No oversized elements.
- No dead-end navigation.

Buttons:
- Use existing module button styles.
- Compact and consistent.

Forms:
- Clear labels and helpful placeholders.
- Inline validation messages.
- Required fields clearly indicated.
- Logical field order.
- Disable invalid submit actions while preserving accessible reason text.
- Enforce safe date-time ordering where required by backend contract, including:
  - `admitted_at <= discharged_at` where both are present
  - `started_at <= ended_at` for ICU stays and ambulance trips
  - `scheduled_from <= scheduled_to` for theatre case filtering contexts

Tier 7 flow UX:
- Selected Tier 7 flow(s) must be self-directed for first-time staff users without a manual.
- Keep primary actions obvious and reduce cognitive load.
- Explain required relationship context in plain language (admission, ICU stay, theatre case, emergency case, ambulance linkage).
- Minimize required data entry to backend-required fields only.
- Preserve selected context while navigating between selected sub-resources.

---

## Security by Design (Mandatory)

- Apply secure-by-default behavior across selected Tier 7 screens/actions.
- Collect only data required for the selected step; do not request unnecessary sensitive data.
- Never expose secrets/tokens/PII in logs, analytics, or UI debug text.
- Prevent duplicate/replay-like submissions with pending/disabled action states.
- Validate and sanitize all route/query params before use:
  - `id`, `tenant_id`, `facility_id`, `patient_id`, `encounter_id`, `admission_id`, `bed_id`, `nurse_user_id`, `prescription_id`, `from_ward_id`, `to_ward_id`, `icu_stay_id`, `theatre_case_id`, `anesthetist_user_id`, `emergency_case_id`, `ambulance_id`, pagination/filter values
- Use existing secure auth/session storage and request utilities.
- Enforce route/action guards before API calls, not only via UI visibility.
- For blocked/unauthorized states, show clear non-technical feedback without leaking sensitive details.

---

## Reliability

- Handle API/network errors gracefully.
- Human-readable, actionable error messages.
- Clear success/failure feedback for every supported action.
- Disable duplicate submissions while pending.
- Preserve user state where possible (filters, selected context, pagination).
- Ensure all enabled actions are fully wired to backend endpoints/contracts (no placeholders).
- Keep behavior robust but simple: prefer existing hooks/components/patterns over new abstractions.
- Use existing query/mutation patterns with proper loading, retry, cancellation, and stale-response safety.

---

## Backend Linkage (Mandatory)

Every implemented feature/action must call real backend endpoints for the selected Tier 7 step(s):

- `11.7.1` IPD (main) (`(main)/ipd`)
  - At minimum: `GET /api/v1/admissions`
  - If showing summary cards, use mounted IPD endpoints (`bed-assignments`, `ward-rounds`, `nursing-notes`, `medication-administrations`, `discharge-summaries`, `transfer-requests`) with valid filters
  - Do not invent aggregate dashboard endpoints

- `11.7.2.1` Admission (`(main)/ipd/admissions`)
  - `GET /api/v1/admissions`
  - `GET /api/v1/admissions/:id`
  - `POST /api/v1/admissions`
  - `PUT /api/v1/admissions/:id`
  - `DELETE /api/v1/admissions/:id`
  - `POST /api/v1/admissions/:id/discharge` (only when implementing explicit discharge action)

- `11.7.2.2` Bed assignment (`(main)/ipd/bed-assignments`)
  - `GET /api/v1/bed-assignments`
  - `GET /api/v1/bed-assignments/:id`
  - `POST /api/v1/bed-assignments`
  - `PUT /api/v1/bed-assignments/:id`
  - `DELETE /api/v1/bed-assignments/:id`

- `11.7.2.3` Ward round (`(main)/ipd/ward-rounds`)
  - `GET /api/v1/ward-rounds`
  - `GET /api/v1/ward-rounds/:id`
  - `POST /api/v1/ward-rounds`
  - `PUT /api/v1/ward-rounds/:id`
  - `DELETE /api/v1/ward-rounds/:id`

- `11.7.2.4` Nursing note (`(main)/ipd/nursing-notes`)
  - `GET /api/v1/nursing-notes`
  - `GET /api/v1/nursing-notes/:id`
  - `POST /api/v1/nursing-notes`
  - `PUT /api/v1/nursing-notes/:id`
  - `DELETE /api/v1/nursing-notes/:id`

- `11.7.2.5` Medication admin (`(main)/ipd/medication-administrations`)
  - `GET /api/v1/medication-administrations`
  - `GET /api/v1/medication-administrations/:id`
  - `POST /api/v1/medication-administrations`
  - `PUT /api/v1/medication-administrations/:id`
  - `DELETE /api/v1/medication-administrations/:id`

- `11.7.2.6` Discharge (`(main)/ipd/discharge-summaries`)
  - `GET /api/v1/discharge-summaries`
  - `GET /api/v1/discharge-summaries/:id`
  - `POST /api/v1/discharge-summaries`
  - `PUT /api/v1/discharge-summaries/:id`
  - `DELETE /api/v1/discharge-summaries/:id`
  - Optional selected-flow linkage to `POST /api/v1/admissions/:id/discharge`

- `11.7.2.7` Transfer (`(main)/ipd/transfer-requests`)
  - `GET /api/v1/transfer-requests`
  - `GET /api/v1/transfer-requests/:id`
  - `POST /api/v1/transfer-requests`
  - `PUT /api/v1/transfer-requests/:id`
  - `DELETE /api/v1/transfer-requests/:id`

- `11.7.3` ICU (main) (`(main)/icu`)
  - At minimum: `GET /api/v1/icu-stays`
  - If showing summary cards, use mounted ICU endpoints (`icu-observations`, `critical-alerts`) with valid filters
  - Do not invent aggregate dashboard endpoints

- `11.7.4.1` ICU stay (`(main)/icu/icu-stays`)
  - `GET /api/v1/icu-stays`
  - `GET /api/v1/icu-stays/:id`
  - `POST /api/v1/icu-stays`
  - `PUT /api/v1/icu-stays/:id`
  - `DELETE /api/v1/icu-stays/:id`

- `11.7.4.2` ICU observation (`(main)/icu/icu-observations`)
  - `GET /api/v1/icu-observations`
  - `GET /api/v1/icu-observations/:id`
  - `POST /api/v1/icu-observations`
  - `PUT /api/v1/icu-observations/:id`
  - `DELETE /api/v1/icu-observations/:id`

- `11.7.4.3` ICU critical alert (`(main)/icu/critical-alerts`)
  - `GET /api/v1/critical-alerts`
  - `GET /api/v1/critical-alerts/:id`
  - `POST /api/v1/critical-alerts`
  - `PUT /api/v1/critical-alerts/:id`
  - `DELETE /api/v1/critical-alerts/:id`

- `11.7.5` Theatre (main) (`(main)/theatre`)
  - At minimum: `GET /api/v1/theatre-cases`
  - If showing summary cards, use mounted theatre endpoints (`anesthesia-records`, `post-op-notes`) with valid filters
  - Do not invent aggregate dashboard endpoints

- `11.7.6.1` Theatre case (`(main)/theatre/theatre-cases`)
  - `GET /api/v1/theatre-cases`
  - `GET /api/v1/theatre-cases/:id`
  - `POST /api/v1/theatre-cases`
  - `PUT /api/v1/theatre-cases/:id`
  - `DELETE /api/v1/theatre-cases/:id`

- `11.7.6.2` Anesthesia record (`(main)/theatre/anesthesia-records`)
  - `GET /api/v1/anesthesia-records`
  - `GET /api/v1/anesthesia-records/:id`
  - `POST /api/v1/anesthesia-records`
  - `PUT /api/v1/anesthesia-records/:id`
  - `DELETE /api/v1/anesthesia-records/:id`

- `11.7.6.3` Post-op note (`(main)/theatre/post-op-notes`)
  - `GET /api/v1/post-op-notes`
  - `GET /api/v1/post-op-notes/:id`
  - `POST /api/v1/post-op-notes`
  - `PUT /api/v1/post-op-notes/:id`
  - `DELETE /api/v1/post-op-notes/:id`

- `11.7.7` Emergency (main) (`(main)/emergency`)
  - At minimum: `GET /api/v1/emergency-cases`
  - If showing summary cards, use mounted emergency endpoints (`triage-assessments`, `emergency-responses`, `ambulances`, `ambulance-dispatches`, `ambulance-trips`) with valid filters
  - Do not invent aggregate dashboard endpoints

- `11.7.8.1` Emergency case (`(main)/emergency/emergency-cases`)
  - `GET /api/v1/emergency-cases`
  - `GET /api/v1/emergency-cases/:id`
  - `POST /api/v1/emergency-cases`
  - `PUT /api/v1/emergency-cases/:id`
  - `DELETE /api/v1/emergency-cases/:id`

- `11.7.8.2` Emergency triage (`(main)/emergency/triage-assessments`)
  - `GET /api/v1/triage-assessments`
  - `GET /api/v1/triage-assessments/:id`
  - `POST /api/v1/triage-assessments`
  - `PUT /api/v1/triage-assessments/:id`
  - `DELETE /api/v1/triage-assessments/:id`

- `11.7.8.3` Emergency response (`(main)/emergency/emergency-responses`)
  - `GET /api/v1/emergency-responses`
  - `GET /api/v1/emergency-responses/:id`
  - `POST /api/v1/emergency-responses`
  - `PUT /api/v1/emergency-responses/:id`
  - `DELETE /api/v1/emergency-responses/:id`

- `11.7.8.4` Ambulance (`(main)/emergency/ambulances`)
  - `GET /api/v1/ambulances`
  - `GET /api/v1/ambulances/:id`
  - `POST /api/v1/ambulances`
  - `PUT /api/v1/ambulances/:id`
  - `DELETE /api/v1/ambulances/:id`

- `11.7.8.5` Dispatch (`(main)/emergency/ambulance-dispatches`)
  - `GET /api/v1/ambulance-dispatches`
  - `GET /api/v1/ambulance-dispatches/:id`
  - `POST /api/v1/ambulance-dispatches`
  - `PUT /api/v1/ambulance-dispatches/:id`
  - `DELETE /api/v1/ambulance-dispatches/:id`

- `11.7.8.6` Trip (`(main)/emergency/ambulance-trips`)
  - `GET /api/v1/ambulance-trips`
  - `GET /api/v1/ambulance-trips/:id`
  - `POST /api/v1/ambulance-trips`
  - `PUT /api/v1/ambulance-trips/:id`
  - `DELETE /api/v1/ambulance-trips/:id`

Keep request/response fields strictly aligned with backend schema/validation rules.
Do not ship mock data, fake success states, or disconnected UI actions.
If backend intentionally lacks support for an action, expose read-only/informational UX and block unsupported writes.

All operations MUST match backend schema and endpoint behavior exactly.

---

## Constraints

Do NOT:

- Modify Tier 1, Tier 2, Tier 3, Tier 4, Tier 5, Tier 6 screens or flows unless required by selected Tier 7 step(s)
- Change layout or routing architecture outside required Tier 7 route wiring
- Add global components or new platform patterns
- Invent backend fields, endpoints, or behaviors
- Implement screens outside selected Tier 7 step(s)

---

## Acceptance Criteria

- 100% rules compliance
- Existing layout patterns reused
- Backend-aligned functionality for selected Tier 7 step(s)
- Responsive UI with clear validation and feedback
- No nested scroll
- Screen(s) work independently and integrate with current main navigation
- Access control is enforced for actions and routes (including deep links)
- Accessibility is preserved for all states, including unauthorized/disabled states
- Selected Tier 7 flow can be completed without a manual
- Selected Tier 7 flow remains secure by default (no data leakage, no insecure shortcuts)
- All applicable features/actions are fully functional and backend-linked
- Unauthorized users cannot execute restricted actions via URL, keyboard, or direct API-triggering UI paths

---

## Deliverables

Return ONLY:

1. Screen/Page component(s) for selected Tier 7 step(s)
2. Form component(s) (if applicable)
3. API integration hook usage/updates (if applicable)
4. Validation logic (if applicable)
5. State handling logic
6. Loading + empty + error + offline UI
7. Success/failure feedback implementation
8. Navigation handlers
9. Backend contract alignment confirmation
10. Access control implementation confirmation (actions + routes)
11. Accessibility control confirmation for all states
12. Route-guard + navigation accessibility confirmation (keyboard/focus/blocked actions)
13. Action-to-backend linkage confirmation for all enabled features in selected screen(s)
14. Explicit note for any selected action intentionally read-only due to missing backend support

Do NOT include files or logic outside selected Tier 7 screen(s).
