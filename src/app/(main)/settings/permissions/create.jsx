/**
 * Permission Create Route
 * (main)/settings/permissions/create
 */
import { PermissionFormScreen } from '@platform/screens';

export default function PermissionCreateRoute() {
  return <PermissionFormScreen />;
}
