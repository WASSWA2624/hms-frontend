/**
 * Address Create Route
 * (main)/settings/addresses/create
 */
import { AddressFormScreen } from '@platform/screens';

export default function AddressCreateRoute() {
  return <AddressFormScreen />;
}
