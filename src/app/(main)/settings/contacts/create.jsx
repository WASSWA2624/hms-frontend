/**
 * Contact Create Route
 * (main)/settings/contacts/create
 */
import { ContactFormScreen } from '@platform/screens';

export default function ContactCreateRoute() {
  return <ContactFormScreen />;
}
