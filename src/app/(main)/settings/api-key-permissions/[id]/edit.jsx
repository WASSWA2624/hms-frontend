/**
 * API Key Permission Edit Route
 * (main)/settings/api-key-permissions/[id]/edit
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { ApiKeyPermissionFormScreen } from '@platform/screens';

export default function ApiKeyPermissionEditRoute() {
  return <ApiKeyPermissionFormScreen />;
}
