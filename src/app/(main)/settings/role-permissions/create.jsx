/**
 * Role Permission Create Route
 * (main)/settings/role-permissions/create
 */
import { RolePermissionFormScreen } from '@platform/screens';

export default function RolePermissionCreateRoute() {
  return <RolePermissionFormScreen />;
}
