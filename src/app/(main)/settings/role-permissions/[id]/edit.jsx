/**
 * Role Permission Edit Route
 * (main)/settings/role-permissions/[id]/edit
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { RolePermissionFormScreen } from '@platform/screens';

export default function RolePermissionEditRoute() {
  return <RolePermissionFormScreen />;
}
