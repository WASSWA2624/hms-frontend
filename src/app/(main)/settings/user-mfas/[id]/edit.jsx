/**
 * User MFA Edit Route
 * (main)/settings/user-mfas/[id]/edit
 * Per app-router.mdc: lightweight page, delegate to platform screen
 */
import { UserMfaFormScreen } from '@platform/screens';

export default function UserMfaEditRoute() {
  return <UserMfaFormScreen />;
}
