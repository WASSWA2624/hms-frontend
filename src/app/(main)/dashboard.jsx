/**
 * Dashboard Route
 * Authenticated dashboard (home) route in the main route group.
 * File: dashboard.jsx
 */
import { DashboardScreen } from '@platform/screens';

export default function DashboardRoute() {
  return <DashboardScreen />;
}
