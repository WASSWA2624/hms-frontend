/**
 * Spacer Types & Constants
 * File: types.js
 */

const AXIS = {
  VERTICAL: 'vertical',
  HORIZONTAL: 'horizontal',
};

const SPACING = {
  XS: 'xs',
  SM: 'sm',
  MD: 'md',
  LG: 'lg',
  XL: 'xl',
  XXL: 'xxl',
};

const AXIS_KEYS = Object.values(AXIS);
const SPACING_KEYS = Object.values(SPACING);

export { AXIS, AXIS_KEYS, SPACING, SPACING_KEYS };


